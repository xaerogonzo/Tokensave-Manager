{
  "mcpServers": {
    "tokensave": {
      "type": "stdio",
      "command": "tokensave",
      "args": ["serve"]
    },
    "some-other-server": {
      "command": "npx",
      "args": ["-y", "@example/mcp"],
      "env": {"EXAMPLE_TOKEN": "x"}
    }
  }
}
